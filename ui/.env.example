# Backend API (Flask). Used for marketplace, auth, and cart.
# Run backend: cd backend && python run.py
# If ui runs on a different port (e.g. 3002), set backend CORS_ORIGINS=http://localhost:3000,http://localhost:3002
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Mahm chat agent (multiagents). Intent classification + tool calls. Run multiagents: cd multiagents && npm run dev (port 3000)
NEXT_PUBLIC_MAHM_URL=http://localhost:3000
